<Window x:Class="MFilesClone.Views.CheckInDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Check In Dokumen" Height="280" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding OriginalFileName, StringFormat='Dokumen: {0}'}" Margin="0,0,0,15" FontWeight="Bold" />

        <TextBlock Grid.Row="1" Text="File pengganti" Margin="0,0,0,3" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox Width="280" Text="{Binding NewFilePath}" IsReadOnly="True" />
            <Button Content="Pilih..." Margin="5,0,0,0" Command="{Binding BrowseFileCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="3" Text="Komentar versi (opsional)" Margin="0,0,0,3" />
        <TextBox Grid.Row="4" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" Height="50" TextWrapping="Wrap" VerticalAlignment="Top" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Width="80" IsDefault="True" Click="OkButton_Click" />
            <Button Content="Batal" Width="80" Margin="10,0,0,0" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
